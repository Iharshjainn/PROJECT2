[build]
  base = "frontend"
  publish = "dist"
  command = "npm run build"

# SPA rewrite rule so React Router routes resolve cleanly on Netlify
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
